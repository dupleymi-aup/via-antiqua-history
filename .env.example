# Auth
JWT_SECRET=change-this-to-a-random-string
DB_PATH=data/app.db

# Site
NEXT_PUBLIC_SITE_URL=https://via-antiqua.maestro7it.ru

# Email (optional - test mode used by default)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@via-antiqua.ru
EMAIL_TEST_MODE=false

# NODE_ENV is set by the runtime (development/production).
# This file documents other configurable variables.
