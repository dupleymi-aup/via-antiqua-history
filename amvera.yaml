meta:
  environment: node
  toolchain:
    name: npm
    version: "22"

build:
  additionalCommands: npm run build && sh scripts/copy-assets.sh

run:
  command: HOST=0.0.0.0 node .next/standalone/server.js
  containerPort: 3000
  persistenceMount: /data
