meta:
  environment: node
  toolchain:
    name: npm
    version: "22"

build:
  additionalCommands: npm run build

run:
  command: HOST=0.0.0.0 npm run start
  containerPort: 3000
  persistenceMount: /data
