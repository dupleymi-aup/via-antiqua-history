meta:
  environment: node
  toolchain:
    name: npm
    version: "22"

build:
  additionalCommands: npm run build

run:
  command: HOST=0.0.0.0 npx next start -p 3000
  containerPort: 3000
  persistenceMount: /data
