meta:
  environment: docker
  toolchain:
    name: docker
    version: "22"

run:
  containerPort: 3000
  persistenceMount: /data

env:
  NODE_ENV: production
  DB_PATH: /data/app.db
  JWT_SECRET: ${JWT_SECRET}
  NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
