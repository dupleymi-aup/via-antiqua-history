meta:
  environment: node
  toolchain:
    name: npm
    version: "22"

build:
  additionalCommands: npm run build

run:
  command: node .next/standalone/server.js
  containerPort: 80
  persistenceMount: /data
